import io;
import Quicksort;
import java.util.List;

class SymbolTable extends Quicksort {

	int intField;
	boolean boolField;
	List listField;

	public int method1() {
	    int intLocal1;
	    boolean boolLocal1;

		intLocal1 = 0 + 10 - 1 * 5 / (3+9);
		boolLocal1 = true;
		listField = new List();

		return 0;
	}

	public boolean method2(int intParam1, boolean boolParam1) {
        boolean boolLocal1;
        boolLocal1 = (1 < 2) && (5 > 4);

		return boolParam1 || boolLocal1;
	}

	public int recursiveMethod(int n) {
	    Test test = new Test();
	    return recursiveMethod(test.foo() + Test.staticFoo());
    }

	public static void main(String[] args) {
	}

}
