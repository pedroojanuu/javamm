MyClass {

    .field public counter.i32;
    .field public isActive.bool;
    .field public message.String;

    .construct MyClass().V {
        invokespecial(this, "").V;
    }

    .method public static main(args.array.String).V {
        test.MyClass :=.MyClass new(MyClass).MyClass;
        invokespecial(test.MyClass,"<init>").V;

        ret.V;
    }
}
